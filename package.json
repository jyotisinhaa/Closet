{
  "name": "closet",
  "version": "1.0.0",
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "start": "cd server && npm install && node index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
